int x;
int y;
int nota;
int aux;
double media;
input("qual o numero de notas: ", x);
while int|aux<<x
{
	input("digita a nota ",nota);
	y = (y+nota);
	aux =(aux+1);
}
media = (y/x);
print("Media: "=>media);
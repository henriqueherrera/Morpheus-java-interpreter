int x;

double y;

int aux;

double notas;

input("numero de notas a ser inseridas: ",x);

while int|aux<<x
{
	input("insira a nota: ",y);

	notas = (notas+y);
	aux = (aux+1);
}
notas = (notas/x);
print("media: "=>notas);
